package online;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;
import java.util.stream.IntStream;
import java.util.stream.Stream;

public class LargestSibling {
	public static void main(String arg[]) {
		/*
		 * int[] array = {7,3,5}; List list=
		 * Stream.of(array).sorted().collect(Collectors.toList()); for(int
		 * i=0;i<list.size();i++) { System.out.println(list.get(i)); }
		 */
		/*
		 * List<Integer> arr = Arrays.asList(1, 2, 3, 4, 5);
		 * System.out.println(arr.stream().filter(e
		 * ->e.equals(8)).findAny().isPresent());
		 */
		/*
		 * IntStream.rangeClosed(3, 9).filter(e->e%2 !=
		 * 0).boxed().collect(Collectors.toList());
		 */
	}
}